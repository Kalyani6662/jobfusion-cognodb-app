Project Title & Use Case: JobFusion (An AI-powered graph-backed job aggregator matching student profiles with enterprise openings using CognoDB).

Why a Graph Database?: Explain why graph databases excel at handling multi-hop traversals and complex skill-node alignments compared to traditional relational models.

Data Model: A brief description or text diagram of nodes and relationships (e.g., (User)-[:HAS_SKILL]->(Skill), (Job)-[:REQUIRES_SKILL]->(Skill)).

Setup & Run Instructions: Steps to clone the repo, install dependencies, configure environment variables (COGNODB_URI, COGNODB_PASSWORD), and run the seed/application scripts.
